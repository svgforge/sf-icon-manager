name: Release to WordPress.org

on:
    push:
        tags:
            - 'v[0-9]*'
            - '[0-9]*'
    workflow_dispatch:
        inputs:
            tag_name:
                description: 'Git tag to attach the GitHub release to'
                required: false
                default: 'v0.2.0'
                type: string

permissions:
    contents: write

jobs:
    release:
        name: Build, deploy and release
        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v7
              with:
                  fetch-depth: 0

            - name: Setup PHP
              uses: shivammathur/setup-php@v2
              with:
                  php-version: '8.4'
                  extensions: mbstring
                  coverage: none
                  tools: composer:v2

            - name: Install PHP dependencies (runtime only)
              run: composer install --no-dev --no-interaction --prefer-dist --no-progress

            - name: Setup pnpm
              uses: pnpm/action-setup@v6

            - name: Setup Node.js
              uses: actions/setup-node@v7
              with:
                  node-version: 24
                  cache: pnpm

            - name: Install dependencies
              run: pnpm install --frozen-lockfile

            - name: Lint, typecheck and unit tests
              run: |
                  pnpm lint:js
                  pnpm run typecheck
                  pnpm run test-unit-js

            - name: Build plugin
              run: pnpm run build

            - name: Build plugin ZIP
              run: pnpm run plugin-zip

            - name: Prune dev dependencies before deploy
              run: composer install --no-dev --no-interaction --prefer-dist --no-progress

            - name: Resolve plugin version
              id: version
              run: echo "version=$(sed -n 's/^Stable tag:[[:space:]]*//p' readme.txt)" >> "$GITHUB_OUTPUT"

            - name: Deploy to WordPress.org
              if: ${{ vars.WP_ORG_DEPLOY == '1' }}
              uses: 10up/action-wordpress-plugin-deploy@stable
              with:
                  generate-zip: true
              env:
                  VERSION: ${{ steps.version.outputs.version }}
                  SVN_USERNAME: ${{ secrets.SVN_USERNAME }}
                  SVN_PASSWORD: ${{ secrets.SVN_PASSWORD }}

            - name: Create GitHub release
              if: ${{ startsWith(github.ref, 'refs/tags/') }}
              uses: softprops/action-gh-release@v3
              with:
                  tag_name: ${{ inputs.tag_name || github.ref_name }}
                  files: ${{ github.workspace }}/${{ github.event.repository.name }}.zip
                  fail_on_unmatched_files: true
